<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lkdml.tp_buffete">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ConActionBar">
        <activity
            android:name=".inicio.Inicio_Login"
            android:theme="@style/ConActionBar"></activity>
        <activity
            android:name=".registro.Registro_Usuario"
            android:theme="@style/ConActionBar" />
        <activity
            android:name=".menu.menu"
            android:label="Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pedido.pedidos"
                    android:label="Mi Pedido"></activity>
    </application>

</manifest>